# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# env files — never commit credentials
.env
.env*.local

# local tooling
/.pgdata/
/.vercel
npm-debug.log*
.DS_Store

# raw image generations live outside the repo; only processed files in public/ are committed
/images-raw/
